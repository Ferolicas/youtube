# Dependencias Python para transcripción (Whisper) y clustering.
# Instalar en un venv:  python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# Transcripción (CPU, int8). large-v3-turbo recomendado para 4 vCPU sin GPU.
faster-whisper==1.0.3

# Clustering temático (TF-IDF + KMeans)
scikit-learn==1.5.2
numpy==2.1.3

# Tendencias Google (OPCIONAL, no oficial, best-effort). Descomenta si lo usas:
# pytrends==4.9.2
